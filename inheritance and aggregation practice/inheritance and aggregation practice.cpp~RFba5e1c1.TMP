#include <iostream>
#include <fstream>
using namespace std;

int main()
{
	ifstream file("inputFile.txt");
	if (file.is_open())
	{
		string word;
		while (file >> word) {
			cout << word << endl;
		}
	}

	return 0;
}
